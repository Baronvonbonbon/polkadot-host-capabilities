<!--
  A sonde report as copied from the app on 2026-09-19, kept as the source for
  runs/2026-09-19_codec1_host-0.19.1_android-16_1406.json (tools/import-sonde-markdown.mjs).
  Complete. The paste repeated its last sections; the repeat is dropped.
  Redacted: the picked file's name. Build 13:26:03Z, opt-in probes not selected.
-->

# sonde report — Pixel 10 Pro XL · Android 16.0.0 · Chromium 152.0.7977.87 · polkadot-app

| | |
|---|---|
| Run | `ac2e906b-dc45-4496-b015-77eab392def1` |
| When | 2026-09-19T14:06:00.501Z |
| Surface | polkadot-app |
| App version | **not exposed by any host API** (system WebView: Chromium 152.0.7977.87) |
| User agent | `Mozilla/5.0 (Linux; Android 16; Pixel 10 Pro XL Build/CP1A.260505.005; wv) AppleWebKit/537.36 (KHTML, like Gecko) Version/4.0 Chrome/152.0.7977.87 Mobile Safari/537.36` |
| Device | Pixel 10 Pro XL · Android 16.0.0 |
| Suite | 1.0.0 (build 2026-09-19T13:26:03.417Z) |
| Source | https://github.com/Baronvonbonbon/sonde |
| truapi | 1 (codec 1) |
| Secure context | true · cross-origin isolated false |
| Max tier | 3 |

## Totals

| pass | fail | timeout | crashed | blocked | unsupported | skip |
|---|---|---|---|---|---|---|
| 89 | 5 | 2 | 0 | 8 | 25 | 22 |

## Needs attention (15)

### `host.payment.balance` — fail (`wrong-result`)

**Payment — balance subscription (read-only)** — Subscribed but no balance arrived in 3 s — not even a zero.

```
A Product cannot show a balance, or decide whether it can afford anything.
```

### `host.cloud.allowance` — timeout (`never-settled`)

**Request a Bulletin allowance** — Never settled after 150s. A call that neither resolves nor rejects is worse than one that fails.

### `host.limits.notifications` — timeout (`never-settled`)

**Notification limits — text length, how many, how far ahead** — Never settled after 120s. A call that neither resolves nor rejects is worse than one that fails.

### `web.compute.serviceWorker` — fail (`threw`)

**Service Worker registration** — ServiceWorker is not available

### `web.storage.cookies` — blocked (`policy-blocked-by-embedder`)

**Cookies + Cookie Store API** — A first-party cookie did not persist. In a framed context this is partitioning or third-party cookie blocking, not a broken engine.

```
navigator.cookieEnabled: true
document.cookie write : did not persist
cookieStore API       : present
cookieStore.getAll    : 0 cookie(s)
hasStorageAccess      : true
framed                : false
```

### `web.net.beacon` — fail (`wrong-result`)

**sendBeacon** — sendBeacon returned false — the request was refused, most likely by CSP connect-src.

### `web.sensors.geolocation` — blocked (`host-callback-missing`)

**Geolocation — one-shot fix** — User denied Geolocation

```
permission before : {"geolocation":"prompt"}
permission after  : {"geolocation":"prompt"}
```

```
error code            : 1
permission before     : prompt
permission after      : prompt
DIAGNOSIS: permission reads 'prompt' yet the request was denied — that is the signature
of a WebView host that never answered the permission callback, NOT a user refusal.
'prompt' means the permission has never been decided: nothing granted, nothing denied,
nothing stored. A genuine refusal leaves 'denied'. So the request was rejected while the
permission remained undecided, which is what Chromium does when a request is raised and
the embedder never resolves it — it fails closed.
On Android the host must implement WebChromeClient.onGeolocationPermissionsShowPrompt
and invoke its callback. The default implementation does nothing at all. Note that
getUserMedia goes through onPermissionRequest, a DIFFERENT callback — so a runtime where
the camera works and location does not is consistent with exactly this gap.
```

### `web.media.pictureInPicture` — blocked (`policy-blocked-by-embedder`)

**Picture-in-Picture** — document.pictureInPictureEnabled is false — the feature is disabled for this document, most likely by permissions-policy.

```
framed                : false
```

### `web.ux.push` — fail (`not-implemented`)

**Push API** — Partially present — 1 of 2 entry points are missing. A half-implemented API is worse than an absent one: feature detection passes and the call fails.

```
PushManager           : absent
navigator.serviceWorker: present (object)
```

### `web.ux.clipboardRead` — blocked (`os-denied`)

**Clipboard read** — Failed to execute 'readText' on 'Clipboard': Read permission denied.

```
permission before : {"clipboard-read":"denied"}
permission after  : {"clipboard-read":"denied"}
```

### `web.ux.fullscreen` — blocked (`policy-blocked-by-embedder`)

**Fullscreen** — document.fullscreenEnabled is false — disabled for this document, most likely by permissions-policy.

```
framed                : false
```

### `web.ux.idleDetection` — blocked (`os-denied`)

**Idle Detection** — Permission is "denied".

```
permission before : {"idle-detection":"denied"}
permission after  : {"idle-detection":"denied"}
```

### `web.ux.contactPicker` — fail (`threw`)

**Contact Picker** — Unable to open a contact selector

### `web.connectivity.nfcRead` — blocked (`os-denied`)

**Web NFC — scan** — Failed to execute 'scan' on 'NDEFReader': NFC permission request denied.

### `web.graphics.webgpu` — blocked (`os-denied`)

**WebGPU adapter + device** — navigator.gpu exists but requestAdapter() returned null — no compatible adapter, or WebGPU is disabled by policy.

```
getPreferredCanvasFormat: rgba8unorm
```


## Everything


### host / system

- `[x]` **Host container detection** — pass (1 ms)
  Running inside the Polkadot host container.
- `[x]` **TrUApi handshake** — pass (74 ms)
  Handshake completed in 72 ms.
- `[-]` **App version — is one exposed at all?** — unsupported (1 ms) · `not-implemented`
  No host API returns an app version. Reports from different builds cannot be attributed to those builds, which is the single largest gap in making these results comparable.
- `[x]` **createApp — construct the SDK App** — pass (106 ms)
  App constructed on the configured environment.
- `[x]` **featureSupported / isChainSupported** — pass (33 ms)
  Queried the host's chain support.
- `[x]` **navigateTo** — pass (31 ms)
  Host accepted the navigation request.

### host / host-storage

- `[x]` **Host local storage — string, JSON, bytes** — pass (49 ms)
  All three value types round-trip.

### host / theme

- `[x]` **Theme subscription** — pass (2501 ms)
  Received 1 theme update(s).

### host / chain

- `[-]` **Chain spec — genesis, name, properties** — unsupported (32869 ms) · `chain-not-supported`
  No candidate chain resolved a spec. T3 probes will refuse for want of an identified chain, which is the intended behaviour.
- `[ ]` **Host JSON-RPC provider** — skip
  Needs "host.chain.genesis", which came back unsupported. Running anyway would test a placeholder.

### host / host-notifications

- `[x]` **Host push notification** — pass (184 ms)
  Host accepted a push notification.

### host / chat

- `[x]` **Chat — list subscription** — pass (2669 ms)
  Chat namespace reachable; 0 room(s) visible.

### host / payment

- `[!]` **Payment — balance subscription (read-only)** — fail (3999 ms) · `wrong-result`
  Subscribed but no balance arrived in 3 s — not even a zero.

### host / preimage

- `[x]` **Preimage lookup (read-only)** — pass (5998 ms)
  Lookup answered.

### host / account

- `[x]` **getUserId** — pass (73 ms)
  Returned a user id.
- `[x]` **Per-index product account derivation** — pass (397 ms)
  4 distinct key(s) across indices — per-index accounts are derivable.
- `[x]` **getLegacyAccounts** — pass (29 ms)
  0 legacy account(s).
- `[x]` **deriveEntropy — determinism** — pass (184 ms)
  Deterministic: the same input gives the same entropy.
- `[x]` **EIP-712 with an app-local burner key** — pass (354 ms)
  Signed and recovered in 20 ms — a contract's ecrecover would accept this.
- `[x]` **wallet.connect + selectAccount** — pass (300 ms)
  Connected and selected an account.
- `[-]` **Ring VRF anonymous alias** — unsupported (5 ms) · `not-implemented`
  getAnonymousAlias() returned null — no alias in this runtime.
- `[x]` **wallet.signMessage** — pass (1336 ms)
  Signed (64 bytes).

### host / host-limits

- `[x]` **Bulletin retention — can earlier uploads still be read?** — pass (125 ms)
  2 of 2 earlier uploads still readable; oldest found 0.0 days.
  measures: `tracked=2` `found=2` `oldestFoundDays=0.03` `youngestMissingDays=-1`

### host / host-cloud

- `[x]` **Cloud storage read (no signature needed)** — pass (52 ms)
  Fetched 0.1 KiB in 52 ms and the CID verifies. Reads work.
- `[x]` **Seal under a crypto-shred key (WebCrypto)** — pass (5 ms)
  Sealed a placeholder in 3 ms (run the camera probe first for a real payload).
- `[x]` **Statement Store — subscribe (read-only)** — pass (6431 ms)
  Subscribed without error; no pages in 6 s.
- `[x]` **Statement Store — createProofAuthorized** — pass (88 ms)
  Host produced an authorized proof.
- `[T]` **Request a Bulletin allowance** — timeout (1436248 ms) · `never-settled`
  Never settled after 150s. A call that neither resolves nor rejects is worse than one that fails.
- `[ ]` **Cloud storage upload (canary, then payload)** — skip · `spend-refused-by-allowlist`
  No genesis hash resolved yet — run host.chain.genesis first. Spending on an unidentified chain is refused.
- `[ ]` **Fetch → CID verify → decrypt** — skip
  Needs "host.cloud.upload", which came back skip. Running anyway would test a placeholder.
- `[ ]` **Preimage submit — upload through the host, then read it back** — skip · `spend-refused-by-allowlist`
  No genesis hash resolved yet — run host.chain.genesis first. Spending on an unidentified chain is refused.
- `[ ]` **Statement Store — submit** — skip · `spend-refused-by-allowlist`
  No genesis hash resolved yet — run host.chain.genesis first. Spending on an unidentified chain is refused.

### host / host-limits

- `[x]` **Largest message the host accepts** — pass (16245 ms)
  Largest message carried both ways: 4 MiB. Beyond it: not tried above 4 MiB (larger values can kill the page — see host.limits.localStorageCeiling).
  measures: `largestOkBytes=4194304` `beyond=not tried above 4 MiB (larger values can kill the page — see host.limits.localStorageCeiling)`
- `[x]` **Many host calls at once** — pass (3245 ms)
  200 calls at once all answered, in 2315 ms.
  measures: `answered_10=10` `ms_10=253` `answered_50=50` `ms_50=562` `answered_200=200` `ms_200=2315`
- `[T]` **Notification limits — text length, how many, how far ahead** — timeout (448742 ms) · `never-settled`
  Never settled after 120s. A call that neither resolves nor rejects is worse than one that fails.
- `[ ]` **Host local storage — largest record** — skip
  Opt-in (crash-risk) — not selected for this run. Run it from its card, or tick the opt-in box.
- `[ ]` **Statement Store — exact size limit** — skip
  Needs "host.statement.submit", which came back skip. Running anyway would test a placeholder.
- `[ ]` **Statement Store — time from submit to delivery** — skip
  Needs "host.statement.submit", which came back skip. Running anyway would test a placeholder.
- `[ ]` **Statement Store — longest expiry** — skip
  Opt-in (spends-quota) — not selected for this run. Run it from its card, or tick the opt-in box.
- `[ ]` **Statement Store — how many one account holds** — skip
  Opt-in (spends-quota) — not selected for this run. Run it from its card, or tick the opt-in box.
- `[ ]` **Largest single Bulletin upload (2, 3, 4 MiB)** — skip
  Opt-in (spends-quota) — not selected for this run. Run it from its card, or tick the opt-in box.
- `[ ]` **Bulletin quota — what happens when it runs out** — skip
  Opt-in (spends-quota) — not selected for this run. Run it from its card, or tick the opt-in box.

### host / permissions

- `[x]` **requestDevicePermission("Location")** — pass (4186 ms)
  Host granted Location. Web geolocation immediately after: still failed — User denied Geolocation
- `[x]` **requestRemotePermission — all five variants** — pass (1371 ms)
  5 of 5 granted.
- `[x]` **requestDevicePermission("Notifications")** — pass (18 ms)
  Host granted Notifications.
- `[x]` **requestDevicePermission("Camera")** — pass (2960 ms)
  Host granted Camera.
- `[x]` **requestDevicePermission("Microphone")** — pass (2345 ms)
  Host granted Microphone.
- `[x]` **requestDevicePermission("Bluetooth")** — pass (1733 ms)
  Host granted Bluetooth.
- `[x]` **requestDevicePermission("NFC")** — pass (919 ms)
  Host granted NFC.
- `[x]` **requestDevicePermission("Clipboard")** — pass (1418 ms)
  Host granted Clipboard.
- `[x]` **requestDevicePermission("OpenUrl")** — pass (912 ms)
  Host granted OpenUrl.
- `[x]` **requestDevicePermission("Biometrics")** — pass (774 ms)
  Host granted Biometrics.

### web / platform

- `[x]` **userAgentData high-entropy hints** — pass (1 ms)
  Device identifies as "Pixel 10 Pro XL".
- `[x]` **Permissions API coverage** — pass (31 ms)
  16 of 20 permission names recognised.
- `[x]` **performance.now() resolution** — pass (1 ms)
  Smallest observable tick: 0.1000 ms.
- `[x]` **Memory measurement** — pass (0 ms)
  Some memory reporting is available.
- `[x]` **Battery Status** — pass (8 ms)
  Exposed — level 79%, on battery.
- `[x]` **Gamepad API** — pass (2 ms)
  API present; nothing connected (expected).
- `[x]` **Web Locks** — pass (29 ms)
  Acquired and released a named lock.
- `[x]` **Prioritised task scheduling** — pass (0 ms)
  Present.
- `[-]` **Speech Synthesis voices** — unsupported (0 ms) · `not-implemented`
  speechSynthesis is not present in this runtime
- `[x]` **Speech Recognition** — pass (0 ms)
  Present.
- `[x]` **Web MIDI (non-sysex)** — pass (12 ms)
  Granted — 0 input(s), 0 output(s).

### web / compute

- `[x]` **WebAssembly compile + instantiate** — pass (2 ms)
  Compiled, instantiated, and executed correctly (compile 1 ms, instantiate 0 ms).
- `[x]` **WASM feature detection (SIMD, threads, exceptions, bulk memory)** — pass (1 ms)
  SIMD available — crypto libraries can take their fast path.
- `[x]` **crypto.subtle algorithm matrix** — pass (75 ms)
  Every algorithm tested works, including Ed25519 and X25519.
- `[x]` **structuredClone type coverage** — pass (3 ms)
  All tested types clone.
- `[x]` **Dedicated Worker round-trip** — pass (13 ms)
  Worker spawned from a blob URL, computed, and replied correctly.
- `[-]` **SharedWorker** — unsupported (0 ms) · `not-implemented`
  Not present in this runtime.
- `[!]` **Service Worker registration** — fail (0 ms) · `threw`
  ServiceWorker is not available
- `[-]` **SharedArrayBuffer + Atomics** — unsupported (0 ms) · `not-implemented`
  SharedArrayBuffer is not defined at all.

### web / storage

- `[x]` **localStorage + sessionStorage round-trip** — pass (2 ms)
  Both stores round-trip.
- `[-]` **IndexedDB round-trip** — unsupported (0 ms) · `not-implemented`
  indexedDB is not present in this runtime
- `[-]` **CacheStorage round-trip** — unsupported (0 ms) · `not-implemented`
  caches is not present in this runtime
- `[x]` **Quota estimate + persistence request** — pass (7 ms)
  Quota reported; storage is best-effort (evictable).
- `[B]` **Cookies + Cookie Store API** — blocked (6 ms) · `policy-blocked-by-embedder`
  A first-party cookie did not persist. In a framed context this is partitioning or third-party cookie blocking, not a broken engine.
- `[x]` **Storage Access API** — pass (0 ms)
  Present.

### web / net

- `[x]` **Permissions-Policy introspection** — pass (1 ms)
  Top-level document; 59 feature(s) allowed.
- `[x]` **Content Security Policy in force** — pass (0 ms)
  No CSP meta tag; any policy is header-delivered and unreadable from script.
- `[x]` **Network Information + online state** — pass (0 ms)
  Link reports as 4g.
- `[x]` **fetch — same-origin, CORS, streaming** — pass (1855 ms)
  Same-origin, cross-origin, and streaming all work.
- `[x]` **WebSocket connect** — pass (1027 ms)
  Connected to wss://rpc.polkadot.io.
- `[!]` **sendBeacon** — fail (2 ms) · `wrong-result`
  sendBeacon returned false — the request was refused, most likely by CSP connect-src.
- `[x]` **Server-Sent Events** — pass (0 ms)
  Present.
- `[x]` **WebTransport** — pass (0 ms)
  Present.

### web / filesystem

- `[x]` **Blob / File / object URL round-trip** — pass (25 ms)
  arrayBuffer, object URL, and stream all return the full payload.
- `[x]` **Origin Private File System** — pass (42 ms)
  Wrote and read back 64.0 KiB byte-for-byte.
- `[x]` **<input type=file> fallback** — pass (3260 ms)
  Received "<picked image>.png".
- `[x]` **File System Access — open picker** — pass (4814 ms)
  Picked "<picked image>.png".
- `[ ]` **File System Access — save picker** — skip (54 ms) · `user-dismissed`
  Dismissed or aborted: Failed to execute 'showSaveFilePicker' on 'Window': The user aborted a request.
- `[ ]` **File System Access — directory picker** — skip (7 ms) · `user-dismissed`
  Dismissed or aborted: Failed to execute 'showDirectoryPicker' on 'Window': The user aborted a request.

### web / sensors

- `[B]` **Geolocation — one-shot fix** — blocked (2 ms) · `host-callback-missing`
  User denied Geolocation
- `[ ]` **Geolocation — watchPosition** — skip
  Needs "web.sensors.geolocation", which came back blocked. Running anyway would test a placeholder.
- `[x]` **DeviceMotion / DeviceOrientation events** — pass (3012 ms)
  Receiving events (177 motion, 119 orientation in 3 s).
- `[x]` **Accelerometer (Generic Sensor)** — pass (105 ms)
  Delivering readings.
- `[x]` **Gyroscope (Generic Sensor)** — pass (104 ms)
  Delivering readings.
- `[-]` **Magnetometer (Generic Sensor)** — unsupported (0 ms) · `not-implemented`
  Magnetometer is absent.
- `[x]` **AbsoluteOrientationSensor (Generic Sensor)** — pass (209 ms)
  Delivering readings.
- `[x]` **RelativeOrientationSensor** — pass (1 ms)
  Present.
- `[-]` **AmbientLightSensor (Generic Sensor)** — unsupported (0 ms) · `not-implemented`
  AmbientLightSensor is absent.

### web / media

- `[x]` **enumerateDevices — labels before grant** — pass (58 ms)
  3 device(s); 0 with labels.
- `[x]` **MediaRecorder codec support** — pass (1 ms)
  6 of 8 formats supported.
- `[x]` **ImageCapture** — pass (0 ms)
  Present.
- `[x]` **Camera capture (getUserMedia → still)** — pass (1254 ms)
  Captured via getUserMedia (480x640, environment camera).
- `[x]` **Microphone capture** — pass (1329 ms)
  Microphone open.
- `[-]` **Screen capture (getDisplayMedia)** — unsupported (1 ms) · `not-implemented`
  mediaDevices.getDisplayMedia is absent — the expected answer on mobile.
- `[B]` **Picture-in-Picture** — blocked (1 ms) · `policy-blocked-by-embedder`
  document.pictureInPictureEnabled is false — the feature is disabled for this document, most likely by permissions-policy.

### web / ux

- `[x]` **Vibration** — pass (0 ms)
  Pattern accepted. (Whether the motor actually fired is not observable from script.)
- `[x]` **Screen Wake Lock** — pass (3 ms)
  Acquired a "screen" wake lock.
- `[-]` **EyeDropper** — unsupported (0 ms) · `not-implemented`
  Not present in this runtime.
- `[x]` **VirtualKeyboard API** — pass (0 ms)
  Present.
- `[x]` **View Transitions** — pass (0 ms)
  Present.
- `[x]` **App Badging** — pass (0 ms)
  Present.
- `[!]` **Push API** — fail (0 ms) · `not-implemented`
  Partially present — 1 of 2 entry points are missing. A half-implemented API is worse than an absent one: feature detection passes and the call fails.
- `[-]` **Notifications** — unsupported (1 ms) · `not-implemented`
  Notification is not present in this runtime
- `[x]` **Clipboard write** — pass (6 ms)
  Wrote text to the clipboard.
- `[B]` **Clipboard read** — blocked (2 ms) · `os-denied`
  Failed to execute 'readText' on 'Clipboard': Read permission denied.
- `[-]` **Web Share — text and URL** — unsupported (1 ms) · `not-implemented`
  navigator.share is not present in this runtime
- `[ ]` **Web Share — files** — skip
  Needs "web.ux.share", which came back unsupported. Running anyway would test a placeholder.
- `[B]` **Fullscreen** — blocked (4 ms) · `policy-blocked-by-embedder`
  document.fullscreenEnabled is false — disabled for this document, most likely by permissions-policy.
- `[-]` **Screen orientation lock** — unsupported (9 ms) · `not-implemented`
  screen.orientation.lock() is not available on this device.
- `[B]` **Idle Detection** — blocked (6 ms) · `os-denied`
  Permission is "denied".
- `[!]` **Contact Picker** — fail (9 ms) · `threw`
  Unable to open a contact selector

### web / identity

- `[-]` **WebAuthn availability** — unsupported (0 ms) · `not-implemented`
  PublicKeyCredential is absent — no WebAuthn.
- `[x]` **Credential Management** — pass (0 ms)
  Present.
- `[-]` **Payment Request — canMakePayment** — unsupported (0 ms) · `not-implemented`
  PaymentRequest is absent.
- `[-]` **Payment Handler** — unsupported (0 ms) · `not-implemented`
  Not present in this runtime.
- `[-]` **FedCM** — unsupported (0 ms) · `not-implemented`
  Not present in this runtime.
- `[-]` **Digital Credentials API** — unsupported (0 ms) · `not-implemented`
  Not present in this runtime.
- `[ ]` **WebAuthn — create a credential** — skip
  Needs "web.identity.webauthnSupport", which came back unsupported. Running anyway would test a placeholder.

### web / connectivity

- `[-]` **Web Bluetooth — device picker** — unsupported (1 ms) · `not-implemented`
  navigator.bluetooth is absent.
- `[-]` **WebUSB — device picker** — unsupported (1 ms) · `not-implemented`
  navigator.usb is absent.
- `[-]` **WebHID — device picker** — unsupported (1 ms) · `not-implemented`
  navigator.hid is absent.
- `[-]` **Web Serial — port picker** — unsupported (1 ms) · `not-implemented`
  navigator.serial is absent — expected on mobile.
- `[B]` **Web NFC — scan** — blocked (10 ms) · `os-denied`
  Failed to execute 'scan' on 'NDEFReader': NFC permission request denied.
- `[ ]` **Web NFC — write** — skip
  Needs "web.connectivity.nfcRead", which came back blocked. Running anyway would test a placeholder.

### web / media

- `[x]` **BarcodeDetector — decode a QR code natively** — pass (202 ms)
  Decoded a QR code in 181 ms; 13 formats supported.
  measures: `decodeMs=181` `formats=13` `decoded=true`

### web / limits

- `[x]` **WebRTC data channel — open, throughput, signalling size** — pass (1785 ms)
  Data channel opened in 1596 ms and moved 1 MiB at 5.6 MiB/s. A non-trickle offer's essentials are 387 B.
  measures: `offerSdpBytes=684` `essentialBytes=387` `candidates=2` `opened=true` `openMs=1596` `mibPerSec=5.56`
- `[x]` **Clipboard — how much text can be copied** — pass (84 ms)
  The clipboard accepted 5000 KB. The clipboard now holds it: paste into Notes to check it ends in "end".
  measures: `largestAcceptedBytes=5000000`
- `[ ]` **Groth16 proof on the phone (a real proximity circuit)** — skip
  Opt-in (slow) — not selected for this run. Run it from its card, or tick the opt-in box.
- `[ ]` **Backgrounding — switch away for 30 s, then come back** — skip
  Opt-in (slow) — not selected for this run. Run it from its card, or tick the opt-in box.

### web / graphics

- `[x]` **Canvas 2D + readback** — pass (1 ms)
  Pixels read back exactly, and export works.
- `[-]` **Image format decoding** — unsupported (5 ms) · `not-implemented`
  Modern format(s) unavailable: avif. Images using them render blank with no error.
- `[x]` **Web Audio** — pass (0 ms)
  Present.
- `[x]` **WebGL 1 + 2** — pass (29 ms)
  WebGL 2.0 (OpenGL ES 3.0 Chromium)
- `[x]` **OffscreenCanvas in a worker** — pass (60 ms)
  Transferred a canvas to a worker and rendered there.
- `[x]` **WebCodecs configuration support** — pass (57 ms)
  10 of 10 configurations supported.
- `[x]` **WebRTC peer connection** — pass (168 ms)
  Gathered 4 ICE candidate(s).
- `[B]` **WebGPU adapter + device** — blocked (24 ms) · `os-denied`
  navigator.gpu exists but requestAdapter() returned null — no compatible adapter, or WebGPU is disabled by policy.

### web / limits

- `[ ]` **WebAssembly memory — how far it grows** — skip
  Opt-in (crash-risk) — not selected for this run. Run it from its card, or tick the opt-in box.
- `[ ]` **JavaScript memory — how much can be allocated** — skip
  Opt-in (crash-risk) — not selected for this run. Run it from its card, or tick the opt-in box.

## Caveats

- No Polkadot App version is recorded because no host API exposes one. The Chromium version is NOT a proxy for it on Android: the `wv` user agent is the system WebView, which the Play Store updates separately from the app.
- A `timeout` means the call never settled. The suite abandons the promise but cannot cancel the underlying platform call — a hung call may still be running.
- Bluetooth/USB/Serial grants persist per origin. Clear site data before re-running, or a second run will report `pass` where a fresh device reports `blocked`.

---

Produced by [sonde](https://github.com/Baronvonbonbon/sonde) — open `sondeprobes.dot` in the Polkadot App
to run it yourself, then diff your report against this one:

```bash

```
