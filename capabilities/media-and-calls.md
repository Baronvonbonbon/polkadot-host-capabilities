---
capability: Camera, microphone, media and peer connections
probes: [web.media.enumerateDevices, web.media.camera, web.media.microphone, web.media.imageCapture, web.media.mediaRecorder, web.media.displayMedia, web.media.pictureInPicture, web.graphics.webAudio, web.graphics.webrtc, web.graphics.webCodecs, web.media.barcodeDetector, web.limits.webrtcLoopback]
---

# Camera, microphone, media and peer connections

**Status (2026-09-19, codec 1, Android): the camera and microphone work through `getUserMedia`.
WebRTC between phones is not measured yet.**

- **Camera:** `getUserMedia({ video: { facingMode: "environment" } })` works, and so does a still at
  480×640. FARE captured 12.3 KiB and compressed it to 3.2 KiB in 952 ms; the canvas re-encode
  strips EXIF.
- **QR codes:** `BarcodeDetector` is built in on Android (almanac P12). Where it's missing (iOS is
  unmeasured), a JavaScript reader is about 62 KiB (`qr` 0.7.0).
- **Microphone:** it opens. `MediaRecorder` supports 6 of 8 tested formats.
- **Device labels** are empty until a grant: 3 devices showed, 0 with labels.
- **Screen capture** is absent, as expected on mobile. **Picture-in-Picture** is disabled for the
  document.

## Peer connections (calls, live data)

`RTCPeerConnection` exists and gathers ICE candidates: 4 in 158 ms on 2026-09-19. **WebCodecs**
supports all 10 tested configurations.

**A data channel opens inside the app** (two peer connections in one page, 2026-09-19 11:31). But
it took **4.4 s** to open, against 0.2 s in desktop Chrome, then 1.6 s at 14:06. It moved 1 MiB at
6.4 and then 5.6 MiB/s. Budget several seconds for a channel to open. A
non-trickle offer cut to its essentials (ufrag, password, fingerprint, 2 candidates) was **387 B**:
it fits in one 512-byte statement, with little to spare. More candidates, such as a TURN relay's,
would push it past one statement. So compress the essentials, or split them across the offer and
the answer.

**QR codes:** `BarcodeDetector` decoded a generated QR code in 174 ms, and lists 13 formats.

What is **not measured**:

- two phones connecting inside the app, after a `WebRtc` grant (almanac P13)
- two phones on different cellular networks, where carrier-grade NAT usually needs a TURN relay

The plan being built is Statement Store signalling with a minified, non-trickle SDP: one
statement per side, replaced in place. See [statement-store.md](statement-store.md) and
[sources/fare.md](../sources/fare.md).

## Measured

<!-- measured:start — generated by tools/build.mjs from runs/; edits here are overwritten -->

| Probe | [2026-09-19_codec1_host-0.19.1_android-16_1406](runs/2026-09-19_codec1_host-0.19.1_android-16_1406.json) | [2026-09-19_codec1_host-0.19.1_android-16_1131](runs/2026-09-19_codec1_host-0.19.1_android-16_1131.json) | [2026-09-19_codec1_host-0.19.1_android-16_1053](runs/2026-09-19_codec1_host-0.19.1_android-16_1053.json) | [2026-09-19_codec1_host-0.19.1_android-16_1038](runs/2026-09-19_codec1_host-0.19.1_android-16_1038.json) (partial) |
|---|---|---|---|---|
| `web.media.enumerateDevices` enumerateDevices — labels before grant | pass | pass | pass | pass |
| `web.media.camera` Camera capture (getUserMedia → still) | pass | pass | pass | pass |
| `web.media.microphone` Microphone capture | pass | pass | pass | pass |
| `web.media.imageCapture` ImageCapture | pass | pass | pass | pass |
| `web.media.mediaRecorder` MediaRecorder codec support | pass | pass | pass | pass |
| `web.media.displayMedia` Screen capture (getDisplayMedia) | unsupported · not-implemented | unsupported · not-implemented | unsupported · not-implemented | unsupported · not-implemented |
| `web.media.pictureInPicture` Picture-in-Picture | blocked · policy-blocked-by-embedder | blocked · policy-blocked-by-embedder | blocked · policy-blocked-by-embedder | blocked · policy-blocked-by-embedder |
| `web.graphics.webAudio` Web Audio | pass | pass | pass | pass |
| `web.graphics.webrtc` WebRTC peer connection | pass | pass | pass | — |
| `web.graphics.webCodecs` WebCodecs configuration support | pass | pass | pass | pass |
| `web.media.barcodeDetector` BarcodeDetector — decode a QR code natively | pass (decodeMs 181, formats 13) | pass (decodeMs 174, formats 13) | — | — |
| `web.limits.webrtcLoopback` WebRTC data channel — open, throughput, signalling size | pass (offerSdpBytes 684, essentialBytes 387, candidates 2) | pass (offerSdpBytes 684, essentialBytes 387, candidates 2) | — | — |

Latest detail per probe:

- `web.media.enumerateDevices` — 3 device(s); 0 with labels.
- `web.media.camera` — Captured via getUserMedia (480x640, environment camera).
- `web.media.microphone` — Microphone open.
- `web.media.imageCapture` — Present.
- `web.media.mediaRecorder` — 6 of 8 formats supported.
- `web.media.displayMedia` — mediaDevices.getDisplayMedia is absent — the expected answer on mobile.
- `web.media.pictureInPicture` — document.pictureInPictureEnabled is false — the feature is disabled for this document, most likely by permissions-policy.

  ```
  framed                : false
  ```
- `web.graphics.webAudio` — Present.
- `web.graphics.webrtc` — Gathered 4 ICE candidate(s).
- `web.graphics.webCodecs` — 10 of 10 configurations supported.
- `web.media.barcodeDetector` — Decoded a QR code in 181 ms; 13 formats supported. — measures: `decodeMs=181` `formats=13` `decoded=true`
- `web.limits.webrtcLoopback` — Data channel opened in 1596 ms and moved 1 MiB at 5.6 MiB/s. A non-trickle offer's essentials are 387 B. — measures: `offerSdpBytes=684` `essentialBytes=387` `candidates=2` `opened=true` `openMs=1596` `mibPerSec=5.56`

<!-- measured:end -->
