---
capability: UI, display and graphics
probes: [web.ux.fullscreen, web.ux.orientationLock, web.ux.wakeLock, web.ux.virtualKeyboard, web.ux.viewTransitions, web.ux.eyeDropper, web.ux.idleDetection, web.ux.contactPicker, web.graphics.canvas2d, web.graphics.imageFormats, web.graphics.webgl, web.graphics.offscreenCanvas, web.graphics.webgpu, web.platform.speechSynthesis, web.platform.speechRecognition, web.platform.userAgentData]
---

# UI, display and graphics

**Status (2026-09-19, codec 1, Android):** Canvas 2D, wake lock, View Transitions and the
VirtualKeyboard API work. Fullscreen is disabled, orientation lock is unavailable, and WebGPU has no
adapter. **AVIF doesn't decode.**

- **AVIF images render blank, with no error.** Serve WebP or JPEG.
- **Fullscreen is disabled** for the document (`fullscreenEnabled` is false), and
  `screen.orientation.lock()` is unavailable. The host owns the chrome.
- **Screen Wake Lock works.** It is useful for a screen showing a QR code, or a driver's
  navigation view.
- **WebGPU:** `navigator.gpu` exists, but `requestAdapter()` returns `null`. WebGL was cut off in
  the 2026-09-19 copy of the report and is not recorded.
- **Speech:** recognition is present. `speechSynthesis` is absent.
- **Contact Picker** is present but throws "Unable to open a contact selector". **Idle Detection**
  is denied.
- `navigator.userAgentData` high-entropy hints give the device model.

## Measured

<!-- measured:start — generated by tools/build.mjs from runs/; edits here are overwritten -->

| Probe | [2026-09-19_codec1_host-0.19.1_android-16](runs/2026-09-19_codec1_host-0.19.1_android-16.json) (partial) |
|---|---|
| `web.ux.fullscreen` Fullscreen | blocked · policy-blocked-by-embedder |
| `web.ux.orientationLock` Screen orientation lock | unsupported · not-implemented |
| `web.ux.wakeLock` Screen Wake Lock | pass |
| `web.ux.virtualKeyboard` VirtualKeyboard API | pass |
| `web.ux.viewTransitions` View Transitions | pass |
| `web.ux.eyeDropper` EyeDropper | unsupported · not-implemented |
| `web.ux.idleDetection` Idle Detection | blocked · os-denied |
| `web.ux.contactPicker` Contact Picker | fail · threw |
| `web.graphics.canvas2d` Canvas 2D + readback | pass |
| `web.graphics.imageFormats` Image format decoding | unsupported · not-implemented |
| `web.graphics.webgl`  | — |
| `web.graphics.offscreenCanvas`  | — |
| `web.graphics.webgpu` WebGPU adapter + device | blocked · os-denied |
| `web.platform.speechSynthesis` Speech Synthesis voices | unsupported · not-implemented |
| `web.platform.speechRecognition` Speech Recognition | pass |
| `web.platform.userAgentData` userAgentData high-entropy hints | pass |

Latest detail per probe:

- `web.ux.fullscreen` — document.fullscreenEnabled is false — disabled for this document, most likely by permissions-policy.
- `web.ux.orientationLock` — screen.orientation.lock() is not available on this device.
- `web.ux.wakeLock` — Acquired a "screen" wake lock.
- `web.ux.virtualKeyboard` — Present.
- `web.ux.viewTransitions` — Present.
- `web.ux.eyeDropper` — Not present in this runtime.
- `web.ux.idleDetection` — Permission is "denied".
- `web.ux.contactPicker` — Unable to open a contact selector
- `web.graphics.canvas2d` — Pixels read back exactly, and export works.
- `web.graphics.imageFormats` — Modern format(s) unavailable: avif. Images using them render blank with no error.
- `web.graphics.webgpu` — navigator.gpu exists but requestAdapter() returned null — no compatible adapter, or WebGPU is disabled by policy.
- `web.platform.speechSynthesis` — speechSynthesis is not present in this runtime
- `web.platform.speechRecognition` — Present.
- `web.platform.userAgentData` — Device identifies as "Pixel 10 Pro XL".

<!-- measured:end -->
